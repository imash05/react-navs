#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel-plugin-react-docgen/node_modules/react-docgen/bin/react-docgen.js" "$@"
  ret=$?
else 
  node  "$basedir/../babel-plugin-react-docgen/node_modules/react-docgen/bin/react-docgen.js" "$@"
  ret=$?
fi
exit $ret
